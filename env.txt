# =============================================================================
# shared-services-classifier — Plantilla de Variables de Entorno
# Ruta: ~/podman/ai-stack/.env (copiar este archivo)
# Marzo 2026
# =============================================================================
# ⚠️  ESTE ARCHIVO (env.txt) ES PÚBLICO — NO CONTIENE VALORES REALES
#     Para desarrollo local, copia: cp env.txt .env
#     Luego edita .env con tus claves VERDADERAS
#     .env está en .gitignore y NUNCA se sube a GitHub
# =============================================================================

# =============================================================================
# OBSERVABILIDAD — LangSmith (Trazabilidad del Agente)
# Obtener claves en: https://smith.langchain.com → Settings → API Keys
# =============================================================================
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=tu_langsmith_api_key_aqui
LANGCHAIN_PROJECT=shared-services-classifier-dev

# =============================================================================
# PROVEEDOR DE IA — Configurable en Runtime
# Valores: ollama | openai | anthropic | gemini
# =============================================================================
AGENT_PROVIDER=ollama
AGENT_MAX_ITERATIONS=5
VALIDATION_ENABLED=true
AGENT_DOMAINS=IT,cliente,operaciones,otro

# Categorías permitidas por dominio (separadas por coma)
CATEGORIES_IT=hardware,software,red,acceso,correo,impresora,vpn,servidor,base_de_datos,seguridad
CATEGORIES_CLIENTE=facturacion,reclamo,consulta,devolucion,garantia,soporte,pedido,envio
CATEGORIES_OPERACIONES=logistica,compras,inventario,mantenimiento,produccion,calidad,proveedores
CATEGORIES_OTRO=general,sin_clasificar

# Umbral de similitud para fuzzy matching (0-100, default 80)
FUZZY_THRESHOLD=80

# Confianza mínima aceptable (0.0-1.0). Por debajo → reintenta clasificación
MIN_CONFIDENCE=0.7

# =============================================================================
# OLLAMA — LLM Local (no requiere API key)
# Modelo: llama3:latest (13GB) o llama3.2:3b (2GB)
# URL accesible desde contenedor: http://ollama:11434
# =============================================================================
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama3.2:3b

# =============================================================================
# OPENAI — LLM by OpenAI (Requiere API Key pagada)
# Obtener en: https://platform.openai.com → API keys
# =============================================================================
OPENAI_API_KEY=sk_test_xxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# ANTHROPIC — LLM by Anthropic (Requiere API Key)
# Obtener en: https://console.anthropic.com → API keys
# =============================================================================
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# =============================================================================
# GOOGLE GEMINI — LLM by Google (Requiere API Key)
# Obtener en: https://ai.google.dev → Get API Key
# =============================================================================
GEMINI_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_MODEL=gemini-1.5-flash

# =============================================================================
# BASE DE DATOS — PostgreSQL
# =============================================================================
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin_password_cambiar_en_produccion
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=classifier_db

# =============================================================================
# CACHE — Redis
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379

# =============================================================================
# AGENTE — FastAPI
# =============================================================================
AGENT_HOST=0.0.0.0
AGENT_PORT=8001
AGENT_TIMEOUT=120

# Modo mock: salta el LLM y usa clasificación fija (solo para desarrollo/pruebas)
# AGENT_MOCK_CLASSIFY=true
# MOCK_DOMINIO=IT
# MOCK_CATEGORIA=acceso
# MOCK_PRIORIDAD=BAJA
# MOCK_CONFIANZA=1.0

# =============================================================================
# ticket-management-backend — Integración externa
#
# Escenario A — SS-TICKET corre en Windows (Podman Desktop):
#   La IP del host Windows es dinámica (cambia entre redes/reinicios).
#   Usar start.sh para levanter el stack — actualiza la IP automáticamente:
#     ~/podman/ai-stack/start.sh
#   O manualmente: TICKET_MGMT_API_URL=http://<IP>:8080/api/v1
#   Obtener IP en WSL: cat /etc/resolv.conf | grep nameserver
#
# Escenario B — ticket-management-backend corre en WSL en la misma shared-network:
#   TICKET_MGMT_API_URL=http://ticket-management-backend:8080/api/v1
#   Requiere: podman network create shared-network (y que el contenedor esté en esa red)
# =============================================================================
TICKET_MGMT_API_URL=http://REEMPLAZAR_CON_IP_WINDOWS:8080/api/v1
TICKET_MGMT_API_KEY=change-this-secret-key-in-production
# Habilitar consulta de catálogo remoto al arrancar (true/false)
# Si false o SS-TICKET no responde → usa CATEGORIES_* del .env como fallback
TICKET_MGMT_CATALOGO_ENABLED=true

# =============================================================================
# MINIO — Almacenamiento de adjuntos
# =============================================================================
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=email-attachments

# =============================================================================
# GRAFANA — Dashboards (Observabilidad)
# =============================================================================
GRAFANA_PASSWORD=admin_password_cambiar_en_produccion

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
# 1. NUNCA subas este archivo con claves reales a Git
# 2. Usa .env (local) para valores verdaderos
# 3. env.txt es solo plantilla PÚBLICA
# 4. En producción, usa variables de entorno o gestores de secretos:
#    - AWS Secrets Manager
#    - HashiCorp Vault
#    - Kubernetes Secrets
#    - Azure Key Vault
# 5. Rota las claves regularmente
# 6. No compartir .env por email/chat/slack
